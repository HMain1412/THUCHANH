<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách người dùng</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            text-align: center; /* Căn giữa toàn bộ nội dung */
        }
        table {
            margin: 0 auto; /* Căn giữa bảng */
            width: 80%; /* Đặt chiều rộng của bảng */
        }
    </style>
</head>
<body>
    <h1>Danh sách người dùng</h1>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>STT</th>
                <th>Username</th>
                <th>Full name</th>
                <th>Address</th>
                <th>Setting</th>
            </tr>
        </thead>
        <tbody>
            <% users.forEach((user, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= user.username %></td>
                    <td><%= user.fullname %></td>
                    <td><%= user.address %></td>
                    <td>
                        <a href="/users/view/<%= user.id %>">👁️</a>
                        <a href="/users/edit/<%= user.id %>">✏️</a>
                        <a href="/users/delete/<%= user.id %>" onclick="return confirm('Are you sure?')">🗑️</a>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>
